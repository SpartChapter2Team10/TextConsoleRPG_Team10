# UIDrawer 렌더링 시스템 흐름도

## 📊 전체 시스템 아키텍처

UIDrawer는 Windows 콘솔 기반의 텍스트 UI 렌더링 시스템으로, 패널 기반 레이아웃과 더블 버퍼링을 통해 깜빡임 없는 부드러운 화면 출력을 제공합니다.

---

## 🎨 시각 자료: 다중 패널 레이아웃 구조

### 화면 분할 개념도

```
콘솔 화면 (106 x 65)
┌────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  Header Panel (0, 0, 106, 5)                                                                       │
│  ┌──────────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  [전투] 1층 - 어두운 복도                                                                     │  │
│  └──────────────────────────────────────────────────────────────────────────────────────────────┘  │
├────────────────────┬──────────────────────────────────────────┬───────────────────────────────────┤
│ Left Panel         │  Center Panel                            │Right Panel                        │
│ (0, 5, 25, 30)     │  (25, 5, 56, 30)                         │  (81, 5, 25, 30)                  │
│ ┌────────────────┐ │  ┌────────────────────────────────────┐  │  ┌─────────────────────────────┐  │
│ │ [플레이어]      │ │  │                                      │  │ [몬스터 정보]                │  │
│ │ 이름: 전사      │ │  │          ⚔️						 │  │  │ 이름: 고블린                 │  │
│ │ HP: 180/200    │ │  │         /|\\						 │  │  │ HP: 80/100                  │  │
│ │ MP: 50/50      │ │  │        / | \\						 │  │  │ 공격력: 15                  │  │
│ │ 공격: 35       │ │  │       [고블린]						 │  │  │ 방어력: 5                   │  │
│ │ 방어: 20       │ │  │    / | \\							 │  │  │                             │  │
│ │ 레벨: 5        │ │  │     /  |  \\						 │  │  │ [예약된 아이템]              │  │
│ │                │ │  │      /___|___\\					 │  │  │ - HP 회복 포션              │  │
│ │ [장비]         │ │  │         ││  │   (조건: HP 30% 이하) │  │
│ │ 무기: 검       │ │  │   (ASCII 아트 애니메이션)			 │  │  │                             │  │
│ │ 방어구: 갑옷   │ │  │									 │  │  │                             │  │
│ └────────────────┘ │  └────────────────────────────────────┘  │  └─────────────────────────────┘  │
├────────────────────┴──────────────────────────────────────────┴───────────────────────────────────┤
│  Bottom Panel (0, 35, 106, 30)                                                                    │
│  ┌──────────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  [전투 로그]																				  │  │
│  │  ────────────────────────────────────────────────────────────────────────────────────────    │  │
│  │  > 전투 시작!																				  │  │
│  │  > 전사의 공격! 고블린에게 35 데미지!														  │  │
│  │  > 고블린의 반격! 전사가 15 데미지를 받았습니다.												  │  │
│  │  > HP 회복 포션이 자동으로 사용되었습니다! HP +50												  │  │
│  │  > 전사의 공격! 고블린을 쓰러뜨렸습니다!														  │  │
│  │  > 승리! 경험치 +100, 골드 +50G																  │  │
│  │																							  │  │
│  └──────────────────────────────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────────────────────────────┘

좌표계: (X, Y, Width, Height)
- Header: (0, 0, 106, 5)
- Left: (0, 5, 25, 30)
- Center: (25, 5, 56, 30)
- Right: (81, 5, 25, 30)
- Bottom: (0, 35, 106, 30)
```

---

## 🔧 ContentRenderer 타입별 구조

### 1. TextRenderer - 텍스트 로그

```
┌─────────────────────────────────────────┐
│  TextRenderer    │
├─────────────────────────────────────────┤
│  _Lines: vector<string>       │  ← 텍스트 라인 저장
│  _TextColor: WORD       │  ← 기본 색상
│  _AutoScroll: bool         │  ← 자동 스크롤 여부
│  _MaxLines: int             │  ← 최대 라인 수
├─────────────────────────────────────────┤
│  AddLine(text)      ───────────┐    │
│  AddLineWithColor(text, color) ────┤    │  메서드
│  Clear()      ───────────┤    │
│  SetAutoScroll(enable)  ───────────┘    │
└─────────────────────────────────────────┘
        │
      ▼ Render()
┌─────────────────────────────────────────┐
│  ScreenBuffer에 출력         │
│  ┌──────────────────────────────────┐   │
│  │ Line 1: "전투 시작!"    │   │
│  │ Line 2: "플레이어 공격!"         │   │
│  │ Line 3: "몬스터 HP -35"          │   │
│  │ ...           │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### 2. StatRenderer - 키-값 쌍

```
┌─────────────────────────────────────────┐
│  StatRenderer       │
├─────────────────────────────────────────┤
│  _Stats: map<string, string>      │  ← 키-값 저장
│    "이름" → "전사" │
│    "HP"   → "180/200"        │
│    "공격" → "35"              │
│  _KeyColor: WORD       │  ← 키 색상 (밝은 회색)
│  _ValueColor: WORD   │  ← 값 색상 (노란색)
├─────────────────────────────────────────┤
│  SetStat(key, value)    ───────────┐    │
│  SetKeyColor(color)     ───────────┤    │  메서드
│  SetValueColor(color)   ───────────┘    │
└─────────────────────────────────────────┘
 │
        ▼ Render()
┌─────────────────────────────────────────┐
│  ScreenBuffer에 출력        │
│  ┌──────────────────────────────────┐   │
│  │ 이름: 전사 (좌측 정렬)    │   │
│  │ HP: 180/200  │   │
│  │ 공격: 35    │   │
│  │ 방어: 20         │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### 3. AsciiArtRenderer - ASCII 아트 + 애니메이션

```
┌─────────────────────────────────────────────────┐
│  AsciiArtRenderer       │
├─────────────────────────────────────────────────┤
│  _ArtLines: vector<string> ← 단일 아트│
│  _AnimationFrames: vector<Frame> ← 애니메이션   │
│  _CurrentFrame: int       ← 현재 프레임  │
│  _FrameTime: float   ← 프레임 시간  │
│  _IsAnimating: bool     ← 재생 중?     │
│  _Alignment: ArtAlignment        ← 정렬 방식    │
├─────────────────────────────────────────────────┤
│  LoadFromFile(path, file)     ──────────┐  │
│  LoadAnimationFromFolder(path, fps) ────┤       │  메서드
│  StartAnimation()       ──────┤  │
│  StopAnimation()        ──────┤       │
│  SetAlignment(align)       ──────┘       │
└─────────────────────────────────────────────────┘
│
   ▼ Update(deltaTime) → 프레임 전환
        ▼ Render()
┌─────────────────────────────────────────────────┐
│  ScreenBuffer에 출력  │
│  ┌───────────────────────────────────────────┐  │
│  │          ⚔️ Frame 1 (0.0s)       │  │
│  │    /|\\          │  │
│  │   / | \\  ──────▶ 0.5s 후     │  │
│  │       [고블린]     │  │
│  │   / | \\                   │  │
│  └───────────────────────────────────────────┘  │
│          │
│  ┌───────────────────────────────────────────┐  │
│  │          🗡️        Frame 2 (0.5s)       │  │
│  │ /|\\  │  │
│  │        / | \\         ──────▶ 0.5s 후 │  │
│  │       [공격!]                │  │
│  │        / | \\          │  │
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

---

## 🎞️ 더블 버퍼링 메커니즘 시각화

### 프레임 N → 프레임 N+1 전환 과정

```
[프레임 N]       [프레임 N+1]
 
  게임 상태 변경                게임 상태 변경
  (플레이어 HP 감소)      (몬스터 사망)
       │       │
       ▼         ▼
┌──────────────────┐       ┌──────────────────┐
│ ScreenBuffer     │        │ ScreenBuffer     │
│ (메모리 버퍼)    │     │ (메모리 버퍼)    │
├──────────────────┤      ├──────────────────┤
│ HP: 180/200      │  │ HP: 165/200    │
│ 고블린 (살아있음)│  │ 고블린 (사망)    │
│ 로그: 공격!      │   │ 로그: 승리!      │
└──────────────────┘   └──────────────────┘
       │   │
       │ WriteChar()      │ WriteChar()
       │ WriteString()           │ WriteString()
       ▼           ▼
┌──────────────────┐     ┌──────────────────┐
│ CHAR_INFO[]      ││ CHAR_INFO[]      │
│ (변환 버퍼)      │  │ (변환 버퍼)    │
├──────────────────┤         ├──────────────────┤
│ UTF-8 → UTF-16   │      │ UTF-8 → UTF-16   │
│ 106x65 배열  │              │ 106x65 배열      │
└──────────────────┘    └──────────────────┘
       │       │
       │ WriteConsoleOutputW()    │ WriteConsoleOutputW()
       │ (단일 API 호출)             │ (단일 API 호출)
       ▼     ▼
┌──────────────────┐     ┌──────────────────┐
│ 콘솔 화면        │    │ 콘솔 화면        │
│ (깜빡임 없음!)   │     │ (부드러운 전환)  │
└──────────────────┘      └──────────────────┘
    30ms 대기       30ms 대기
       │    │
       └──────────────────────────────────┘
           다음 프레임으로...
```

**핵심 포인트:**
- ✅ **메모리에서 모든 작업 완료** → 화면에 한 번에 출력
- ✅ **깜빡임 제거**: 픽셀 단위가 아닌 프레임 단위 교체
- ✅ **일관된 FPS**: Sleep으로 타이밍 제어

---

## 🔄 Dirty Flag 시스템 동작 원리

### 변경 감지 및 최적화

```
프레임 N
┌─────────────┬─────────────┬─────────────┐
│ Panel A     │ Panel B     │ Panel C     │
│ Dirty: TRUE │ Dirty: FALSE│ Dirty: TRUE │
│ (플레이어)  │ (배경 아트) │ (로그)      │
└─────────────┴─────────────┴─────────────┘
  │       │         │
       ▼  │          ▼
   렌더링 ✅Skip ⏭️     렌더링 ✅
       │  │     │
       ▼       │         ▼
┌─────────────┐│       ┌─────────────┐
│ HP: 180/200 │       │   │ > 공격!     │
│ ───────────▶│       │       │ > HP -15    │
│ HP: 165/200 │       │       └─────────────┘
└─────────────┘       │
   │              │   │
       ▼      ▼            ▼
   Dirty = FALSE  (변경 없음)   Dirty = FALSE
       │            │              │
       └──────────────┴──────────────┘
     │
   ▼
        ScreenBuffer에 병합
      │
        ▼
       WriteConsoleOutputW()
```

**최적화 효과:**
```
일반 렌더링: Panel A + B + C = 3개 처리
Dirty 적용: Panel A + C = 2개 처리 (33% 절약!)

전투 중 애니메이션만 변경되는 경우:
- 변경: 1개 Panel (몬스터 아트)
- 스킵: 4개 Panel (플레이어, 로그, 헤더, 배경)
→ CPU 사용 80% 감소!
```

---

## 📦 UTF-8 문자 처리 상세도

### 한글 "안녕" 렌더링 과정

```
입력: "안녕" (UTF-8)
│
├─ "안" = E5 88 88 (3바이트)
│   ├─ ParseUTF8Char()
│   │   └─ byteCount = 3
│   │   └─ visualWidth = 2 (2칸 차지)
│   │
│   ├─ ScreenBuffer::WriteChar(x=0, y=0)
│   │   ┌────────────────────────────┐
│   │   │ _Buffer[0][0]      │
│   │   │ ┌────────────────────────┐ │
│   │   │ │ Bytes: [E5][88][88][00]│ │
│   │   │ │ ByteCount: 3           │ │
│   │   │ │ VisualWidth: 2   │ │
││   │ │ Attribute: 0x07        │ │
│   │   │ └────────────────────────┘ │
│   │   └────────────────────────────┘
│   │   ┌────────────────────────────┐
│   │   │ _Buffer[0][1] (마커)       │
│   │   │ ┌────────────────────────┐ │
│   │   │ │ Bytes: [00]   │ │
│   │   │ │ ByteCount: 0  │ │
│   │   │ │ VisualWidth: 0     │ │
│   │   │ └────────────────────────┘ │
│   │   └────────────────────────────┘
│   │
│   └─ Render() → UTF-16 변환
│    MultiByteToWideChar(CP_UTF8, ...)
│     "안" → U+C548 (wchar_t)
│       │
│       ├─ CHAR_INFO[0]
│       │   UnicodeChar: 0xC548
│   │   Attributes: LEADING_BYTE | 0x07
│       │
│       └─ CHAR_INFO[1]
│           UnicodeChar: 0xC548
│     Attributes: TRAILING_BYTE | 0x07
│
└─ "녕" = EB 85 95 (3바이트)
    └─ (동일한 과정 x=2, x=3에서 반복)

최종 콘솔 출력:
┌───┬───┬───┬───┐
│ 안│   │ 녕│   │  ← 시각적으로 2칸씩 차지
└───┴───┴───┴───┘
  0   1   2   3← 실제 배열 인덱스
```

**ASCII vs 한글 비교:**
```
ASCII "Hi":
  WriteChar('H') → 1칸 │ WriteChar('i') → 1칸
  ScreenBuffer[0] = 'H' │ ScreenBuffer[1] = 'i'
  
한글 "안":
  WriteChar("안") → 2칸
  ScreenBuffer[0] = "안" (LEADING)
  ScreenBuffer[1] = (마커) (TRAILING)
```

---

## 🎬 애니메이션 프레임 전환 메커니즘

### 시간 기반 프레임 업데이트

```
AsciiArtRenderer (FPS = 2.0, 즉 0.5초/프레임)

프레임 배열:
┌───────────────────────────────────────┐
│ Frames[0]: 고블린_대기1.txt           │
│ Frames[1]: 고블린_대기2.txt           │
│ Frames[2]: 고블린_공격.txt   │
│ Frames[3]: 고블린_피격.txt        │
└───────────────────────────────────────┘

시간 흐름:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━▶
0.0s    0.5s    1.0s    1.5s    2.0s
 │       │    │       │       │
 │       │       │     │    └─ Frame 0 (반복)
 │       │       │       └─ Frame 3
 │       │       └─ Frame 2
 │ └─ Frame 1
 └─ Frame 0

Update(deltaTime) 호출마다:
┌─────────────────────────────────────┐
│ _FrameTimer += deltaTime       │
│ │
│ if (_FrameTimer >= _FrameTime) {    │
│     _CurrentFrame++;        │
│     _CurrentFrame %= _FrameCount;   │
│     _FrameTimer -= _FrameTime;      │
│     SetDirty();  // 재렌더링 요청   │
│ }        │
└─────────────────────────────────────┘

Render() 호출 시:
  현재 프레임의 아트를 ScreenBuffer에 출력
  
실제 화면:
  T=0.0s: 대기 자세 1
  T=0.5s: 대기 자세 2
  T=1.0s: 공격 모션
  T=1.5s: 피격 모션
  T=2.0s: 대기 자세 1 (반복)
```

**deltaTime 기반 업데이트 장점:**
```
고정 시간 방식 (BAD):
  Update() → 항상 다음 프레임
  문제: 렌더링 속도에 따라 애니메이션 속도 달라짐
  
deltaTime 방식 (GOOD):
  Update(0.016s) → 누적 시간 체크 → 정확한 타이밍
  장점: FPS와 무관하게 일정한 애니메이션 속도
```

---

## 🔍 실전 예제: 전투 화면

### 화면 구성
```
┌──────────────────────────────────────────────────────────────┐
│   [전투] 1층 - 어두운 복도  │  ← Header Panel
├──────────────┬──────────────────────────┬────────────────────┤
│ [플레이어]   │      │   [몬스터]  │
│ 이름: 전사   │ ⚔️ 고블린   │   이름: 고블린  │
│ HP: 180/200  │    (ASCII 아트)  │   HP: 80/100       │
│ 공격: 35     ││   공격: 15      │
└──────────────┴──────────────────────────┴────────────────────┤
│    [전투 로그]          │
│ > 전투 시작!     │
│ > 전사의 공격! 고블린에게 35 데미지!      │
│ > 고블린의 반격! 전사가 15 데미지를 받았습니다.   │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎯 실전 활용 패턴

### 패턴 1: 동적 스탯 업데이트 (실시간 HP 변경)

```cpp
// BattleScene에서 매 턴마다 호출
void UpdatePlayerHP(Player* player)
{
 // 1. 패널 가져오기
Panel* statsPanel = _Drawer->GetPanel("PlayerStats");
    if (!statsPanel) return;
    
    // 2. StatRenderer 캐스팅
    StatRenderer* renderer = dynamic_cast<StatRenderer*>(
    statsPanel->GetContentRenderer()
 );
    if (!renderer) return;
    
    // 3. HP 값만 업데이트
    renderer->SetStat("HP", 
        std::to_string(player->GetCurrentHP()) + "/" + 
     std::to_string(player->GetMaxHP())
    );
    
 // 4. 재렌더링 표시
    statsPanel->Redraw();
    
    // 5. 화면 갱신 (다음 Update()에서 자동)
    // _Drawer->Update()가 게임 루프에서 호출됨
}
```

**동작 흐름:**
```
플레이어 공격받음
  └─▶ HP 변경 (200 → 185)
    └─▶ UpdatePlayerHP() 호출
         └─▶ SetStat("HP", "185/200")
       └─▶ panel->Redraw() → _IsDirty = true
    └─▶ _Drawer->Update()
          └─▶ Dirty 감지
   └─▶ 해당 패널만 렌더링 ✅
         └─▶ 화면 즉시 반영
```

---

### 패턴 2: 전투 로그 누적 (자동 스크롤)

```cpp
// 전투 로그 추가 헬퍼 함수
void AddBattleLog(const std::string& message, ETextColor color = ETextColor::WHITE)
{
    Panel* logPanel = _Drawer->GetPanel("BattleLog");
    if (!logPanel) return;
  
    TextRenderer* renderer = dynamic_cast<TextRenderer*>(
      logPanel->GetContentRenderer()
  );
    if (!renderer) return;
    
    // 색상 있는 로그 추가
    renderer->AddLineWithColor(message, static_cast<WORD>(color));
    
    // 자동 스크롤 활성화 (최신 로그가 보이도록)
    renderer->SetAutoScroll(true);
    
 logPanel->Redraw();
}

// 사용 예시
void ProcessAttack(ICharacter* attacker, ICharacter* defender)
{
    int damage = CalculateDamage(attacker, defender);
    defender->TakeDamage(damage);
    
    // 공격 로그 (노란색)
    AddBattleLog(
        attacker->GetName() + "의 공격! " + 
     std::to_string(damage) + " 데미지!",
     ETextColor::LIGHT_YELLOW
    );
    
    // HP 위험 경고 (빨간색)
    if (defender->GetCurrentHP() < defender->GetMaxHP() * 0.3) {
        AddBattleLog(
        defender->GetName() + "의 HP가 위험합니다!",
            ETextColor::LIGHT_RED
 );
    }
}
```

**로그 누적 동작:**
```
┌────────────────────────────────┐
│ [전투 로그]        │  ← 최대 20줄 제한
├────────────────────────────────┤
│ 1. 전투 시작!       │
│ 2. 플레이어 공격! 35 데미지    │
│ 3. 몬스터 반격! 15 데미지      │
│ 4. HP 회복 포션 사용!          │
│ 5. ...        │
│ ...          │
│ 20. 승리! │  ← 최신 로그
└────────────────────────────────┘
        │
AddLine() 호출
   │
 ▼
┌────────────────────────────────┐
│ 2. 플레이어 공격!   │  ← 1번 삭제됨
│ 3. 몬스터 반격!     │
│ ...        │
│ 20. 승리!          │
│ 21. 아이템 획득!    │  ← 새로 추가
└────────────────────────────────┘
```

---

### 패턴 3: 애니메이션 상태 전환 (공격 → 대기)

```cpp
// 몬스터 애니메이션 제어
class MonsterAnimationController
{
private:
    UIDrawer* _Drawer;
    std::string _PanelID;
    
public:
    // 공격 애니메이션 재생
    void PlayAttack()
    {
     Panel* panel = _Drawer->GetPanel(_PanelID);
if (!panel) return;
        
        AsciiArtRenderer* art = dynamic_cast<AsciiArtRenderer*>(
         panel->GetContentRenderer()
        );
        if (!art) return;
      
        // 공격 애니메이션 로드 (빠른 속도)
        DataManager* dm = DataManager::GetInstance();
        art->LoadAnimationFromFolder(
            dm->GetResourcePath("Animations") + "/MonsterAttack",
            5.0f  // 5 FPS (0.2초/프레임)
        );
        art->StartAnimation();
 panel->Redraw();
    }
    
    // 대기 애니메이션으로 복귀
    void ReturnToIdle()
    {
        Panel* panel = _Drawer->GetPanel(_PanelID);
        if (!panel) return;
 
 AsciiArtRenderer* art = dynamic_cast<AsciiArtRenderer*>(
     panel->GetContentRenderer()
     );
        if (!art) return;
        
        // 대기 애니메이션 로드 (느린 속도)
  DataManager* dm = DataManager::GetInstance();
        art->LoadAnimationFromFolder(
            dm->GetResourcePath("Animations") + "/MonsterIdle",
            2.0f  // 2 FPS (0.5초/프레임)
 );
      art->StartAnimation();
     panel->Redraw();
    }
};

// 전투에서 사용
void ExecuteMonsterAttack(Monster* monster, Player* player)
{
    // 1. 공격 애니메이션 시작
    animController.PlayAttack();
    
    // 2. 애니메이션 재생 (1초)
    Sleep(1000);
    
    // 3. 데미지 처리
    ProcessDamage(monster, player);
    
    // 4. 대기 애니메이션으로 복귀
    animController.ReturnToIdle();
}
```

**애니메이션 전환 타임라인:**
```
시간축: ━━━━━━━━━━━━━━━━━━━━━━━━━━━━▶
        0.0s  0.2s  0.4s  0.6s  0.8s  1.0s  1.2s

공격 애니메이션 (5 FPS):
        ⚔️1   ⚔️2   ⚔️3   ⚔️4   ⚔️5   ◀─ PlayAttack()
        │    │ │    │    │
     └────┴────┴────┴────┴─ Sleep(1000)
       │
    ▼
대기 애니메이션 (2 FPS):
      🧍1        🧍2     ◀─ ReturnToIdle()
      │         │
        0.5초     1.0초
```

---

### 패턴 4: 다중 패널 일괄 업데이트

```cpp
// 전투 UI 전체 업데이트 (한 프레임에 여러 패널 변경)
void RefreshBattleUI(Player* player, Monster* monster)
{
    // ===== 여러 패널 업데이트 (Redraw만 호출) =====
    
    // 1. 플레이어 스탯
    UpdatePlayerStats(player);  // 내부에서 Redraw() 호출
    
    // 2. 몬스터 스탯
    UpdateMonsterStats(monster);  // 내부에서 Redraw() 호출
    
    // 3. 전투 로그
    AddBattleLog("턴 종료");  // 내부에서 Redraw() 호출
    
// 4. 예약된 아이템 목록
    UpdateReservedItems(player);  // 내부에서 Redraw() 호출
    
    // ===== 마지막에 한 번만 렌더링! (성능 최적화) =====
    _Drawer->Render();  // 모든 Dirty 패널을 한 번에 처리
}
```

**일괄 업데이트 성능 비교:**
```
❌ 나쁜 방법 (비효율):
  UpdatePlayerStats(player);
  _Drawer->Render();  // 1번 렌더링
  UpdateMonsterStats(monster);
  _Drawer->Render();  // 2번 렌더링
  AddBattleLog("턴 종료");
  _Drawer->Render();  // 3번 렌더링
  → 총 3번 WriteConsoleOutputW 호출 (느림!)

✅ 좋은 방법 (효율):
  UpdatePlayerStats(player);    // Redraw만
  UpdateMonsterStats(monster);  // Redraw만
  AddBattleLog("턴 종료");      // Redraw만
  _Drawer->Render();  // 1번 렌더링 (한 번에!)
  → 총 1번 WriteConsoleOutputW 호출 (3배 빠름!)
```

---

### 패턴 5: 조건부 렌더링 (값 변경 시에만)

```cpp
// HP가 실제로 변경된 경우에만 업데이트
class SmartStatsUpdater
{
private:
int _LastHP = -1;
    int _LastMP = -1;
    
public:
    void UpdateIfChanged(Player* player)
    {
        int currentHP = player->GetCurrentHP();
        int currentMP = player->GetCurrentMP();
    
        // HP가 변경되었을 때만 업데이트
        if (currentHP != _LastHP || currentMP != _LastMP) {
            UpdatePlayerStats(player);
    _LastHP = currentHP;
  _LastMP = currentMP;
        }
        // 변경 없으면 Skip! (성능 향상)
    }
};
```

**조건부 업데이트 효과:**
```
프레임 1: HP 200 → UpdateIfChanged() → 렌더링 ✅
프레임 2: HP 200 (변경 없음) → Skip ⏭️
프레임 3: HP 200 (변경 없음) → Skip ⏭️
프레임 4: HP 200 (변경 없음) → Skip ⏭️
프레임 5: HP 185 (변경!) → UpdateIfChanged() → 렌더링 ✅

결과: 5 프레임 중 2번만 렌더링 (60% CPU 절약!)
```

---

### 패턴 6: 패널 재사용 (동적 내용 교체)

```cpp
// 같은 패널에 다른 몬스터 아트 로드
void SwitchMonster(const std::string& newMonsterName)
{
    Panel* monsterPanel = _Drawer->GetPanel("MonsterArt");
    if (!monsterPanel) return;
    
    // ===== 방법 1: 기존 렌더러 재사용 =====
    AsciiArtRenderer* art = dynamic_cast<AsciiArtRenderer*>(
        monsterPanel->GetContentRenderer()
    );
    if (art) {
      // 새 아트 로드
        DataManager* dm = DataManager::GetInstance();
        art->LoadFromFile(
    dm->GetResourcePath("Monsters"),
newMonsterName + ".txt"
        );
        art->StopAnimation();  // 기존 애니메이션 중지
        monsterPanel->Redraw();
    }

    // ===== 방법 2: 렌더러 완전 교체 =====
auto newArt = std::make_unique<AsciiArtRenderer>();
    DataManager* dm = DataManager::GetInstance();
    newArt->LoadAnimationFromFolder(
        dm->GetResourcePath("Animations") + "/" + newMonsterName + "Idle",
        2.0f
    );
    newArt->SetAlignment(ArtAlignment::CENTER);
    newArt->SetColor(ETextColor::LIGHT_RED);
  newArt->StartAnimation();
    
  monsterPanel->SetContentRenderer(std::move(newArt));
    // SetContentRenderer가 자동으로 Redraw() 호출
}
```

**패널 재사용 시나리오:**
```
보스전 진입
  └─▶ SwitchMonster("Boss_Dragon")
       ├─ 고블린 아트 제거
 └─ 드래곤 아트 로드
         └─ 같은 Panel ID 사용 (레이아웃 유지)

전투 승리 후 다음 몬스터
  └─▶ SwitchMonster("Orc")
       └─ 드래곤 아트 제거
 └─ 오크 아트 로드
```

---

## 🎓 실전 체크리스트

### Scene 구현 시 필수 확인사항

**초기화 단계:**
- [ ] `UIDrawer::Initialize()` 호출했는가?
- [ ] `UIDrawer::Activate()` 호출했는가?
- [ ] 모든 패널에 고유한 ID를 부여했는가?
- [ ] 패널 좌표가 화면 범위 내인가? (106x65)

**패널 생성 단계:**
- [ ] `CreatePanel()` 반환값이 nullptr인지 확인했는가?
- [ ] ContentRenderer를 `std::move()`로 전달했는가?
- [ ] 테두리가 필요한 패널에 `SetBorder()` 호출했는가?

**업데이트 단계:**
- [ ] 게임 루프에서 `Update()` 호출하는가?
- [ ] 내용 변경 후 `panel->Redraw()` 호출하는가?
- [ ] `dynamic_cast` 후 nullptr 체크했는가?

**성능 최적화:**
- [ ] 여러 패널 업데이트 후 `Render()` **한 번만** 호출하는가?
- [ ] 변경되지 않은 데이터는 Skip하는가?
- [ ] 애니메이션이 필요 없는 패널에 정적 아트 사용하는가?

**종료 단계:**
- [ ] Scene 종료 시 `UIDrawer::Deactivate()` 호출했는가?
- [ ] 패널 메모리 누수가 없는가? (unique_ptr 사용)

---

## 💡 성능 최적화 팁 요약

| 기법 | 설명 | 효과 |
|------|------|------|
| **Dirty Flag** | 변경된 패널만 렌더링 | CPU 30~80% 절감 |
| **더블 버퍼링** | 메모리에서 작업 → 한 번에 출력 | 깜빡임 제거 |
| **일괄 업데이트** | 여러 패널 변경 후 Render() 1회 | 렌더링 속도 3~5배 |
| **조건부 업데이트** | 값 변경 시에만 SetStat() | CPU 40~60% 절감 |
| **애니메이션 제한** | 필요한 패널에만 애니메이션 | 메모리 절약 |
| **FPS 제한** | Sleep으로 프레임 타이밍 제어 | CPU 사용률 안정화 |
